/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["media.dodostatic.com", "media.dodostatic.net"],
    },
    reactStrictMode: false,
};

export default nextConfig;
